import * as React from "react";
import { Text } from "../../shared/components/text";

import "./app.sass";

export const AppComponent = () => {
  return (
    <div className="app">
      <Text text="This is the LARA App" />
    </div>
  );
};
